// This targets the <p> element and removes it from the DOM
// Only finds the first matching target
// let mySelection = document.querySelector('p')
// mySelection.remove()

// Selects all with the <p> element
/* let mySelection = document.querySelectorAll('p')

mySelection.forEach(function(paragraph) {
    // paragraph.remove()
    
    // Remove the <p> element that has the string in it
    // if (paragraph.textContent.includes('***')) {
    //    paragraph.remove()
    //} 

    // Javascript can change the innerHTML of elements
    // paragraph.textContent = "This is from the javascript code"
}) */

// Change the first element of the "array" within the body
// document.body.children[0].textContent = "Hello from javascript"

// Change the text color to blue
document.body.children[0].style.color = "blue"